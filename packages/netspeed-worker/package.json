{
    "name": "@netspeed/worker",
    "author": "Anatoly Ktitarov <hello@ktitarov.com>",
    "description": "cURL metrics collector for the netspeed",
    "version": "0.0.0",
    "private": true,
    "main": "dist/index.js",
    "scripts": {
        "start": "node ./dist",
        "clean": "rimraf ./dist",
        "build": "node ./esbuild",
        "prebuild": "npm run clean"
    },
    "devDependencies": {
        "@types/node": "^20.11.16",
        "esbuild": "^0.20.0",
        "rimraf": "^5.0.5",
        "typescript": "^5.3.3"
    }
}
